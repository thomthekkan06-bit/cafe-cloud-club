/* --- PRELOADER SCRIPT --- */
window.addEventListener('load', () => {
    const preloader = document.getElementById('preloader');
    setTimeout(() => {
        preloader.classList.add('preloader-hidden');
    }, 2000); // 2 seconds delay
});

/* --- PASTE YOUR ORIGINAL JS BELOW THIS LINE --- */
/* Copy everything BETWEEN the <script> and </script> tags from your original code */
/* IMPORTANT: Do not include the <script> tags themselves. Just the code inside. */